# Hugging Face (필수: API 모드 또는 모델/어댑터 다운로드)
HF_API_KEY=your_huggingface_token_here

# Gemini Judge (eval/run_benchmark.py)
GENAI_API_KEY=your_gemini_api_key_here

# 공통 모델 설정 (평가 모듈 기본값)
BASE_MODEL_NAME=Qwen/Qwen2.5-14B-Instruct
EMBEDDING_MODEL_NAME=BAAI/bge-m3

# Tavily 웹 검색 (선택)
TAVILY_API_KEY=your_tavily_key_here

# 임베딩: local(로컬 bge-m3) / api
EMBEDDING_MODE=local

# LLM: api(라우팅 API + 답변 로컬 권장) / vessel(라우팅+답변 둘 다 로컬, 24GB 단일 GPU 시 OOM 가능)
LLM_MODE=api

# vessel 시 양자화 (4bit 권장)
LLM_4BIT=1
LLM_QUANT=8bit
LLM_8BIT=1

# VRAM 부족 시 8bit 일부 CPU 오프로드 (단일 GPU에서 14B 8bit 시)
# LLM_CPU_OFFLOAD=1

# Attention 구현: sdpa(기본) / flash_attention_2 (CUDA 호환 시)
# ATTN_IMPLEMENTATION=sdpa

# LangSmith (선택)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=...
# LANGCHAIN_ENDPOINT=...
# LANGCHAIN_PROJECT=...
